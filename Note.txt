
// PipeWire (dc QT uu tien)
sudo apt install pipewire \
                 pipewire-audio-client-libraries \
                 wireplumber \
                 pipewire-pulse

wpctl status
wpctl set-default <node_id>


1. Dưới item bluetooth sẽ là 
Add Device. Sau đó hiển thị model để tìm kiếm device bluetooth mới

Danh sách các device đã từng kết nối -> pair hay không, chọn thằng để pair
(thằng pair mà là Sink tức Audio phát thì cho nó hiển thị bên danh sách chọn Audio device và ưu tiên nó luôn)
