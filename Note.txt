1. Thông báo dài chữ sẽ bị tràn ra ngoài
Ngoài ra chữ thông báo nên để bé hơn chút

2. Khi chơi hết bài nó quay về position 0, nhưng ko thể chơi lại tiếp được. 
Có vẻ là trạng thái chưa chuyển đúng, cần cho nhạc chơi ms biết được

3. Ko detect đc Audio Device 
QAudioDeviceInfo::availableDevices(QAudio::AudioOutput)

4. Nút nhấn méo kêu, có vẻ thiếu AudioOutput gì đó cho thằng đó rồi

/////////////////////////////////////////////////////////////////////////////

Khi bấm vào item khác thì sẽ ngừng chơi luôn và thoát ra (reset trạng thái slider, time)
Khi bắt đầu record thì cũng vậy
Và khi start chơi một item thì lại CANCEL cái đang record

Xài thư viện của QT mà chơi nhạc, control slider 
Có thể là GStreamer ? một multimedia framework chuyên để xây dựng pipeline xử lý video/audio streaming, playback, encode/decode.
Hoặc xài mẹ QtMultimedia

OpenCV là một computer vision library tập trung vào xử lý ảnh, AI, nhận diện đối tượng. → Nếu bạn muốn truyền/stream video/audio 
thì GStreamer mạnh hơn; nếu bạn muốn phân tích nội dung hình ảnh thì OpenCV phù hợp hơn.

QtMultimedia không tự viết lại toàn bộ multimedia stack, mà nó dựa trên backend khác nhau tùy hệ điều hành:
Trên Linux (như Raspberry Pi) → QtMultimedia thường dùng GStreamer làm backend.

-> Khả năng sẽ xài GStreamer + OpenCV
Xài QtMultimedia tức là đang xài GStreamer rồi

Đúng rồi 👍. Trong Qt 6, QAudioOutput là lớp chuyên để phát luồng âm thanh thô (PCM) ra loa, còn QAudioSource là lớp để thu âm từ micro.

Nếu bạn muốn làm ứng dụng kiểu “call” (thu từ mic rồi phát trực tiếp ra loa hoặc gửi qua mạng), thì cặp đôi QAudioSource + QAudioOutput là lựa chọn chuẩn:
QAudioSource: lấy dữ liệu từ micro, trả về QIODevice để bạn đọc dữ liệu PCM.
QAudioOutput: nhận dữ liệu PCM và phát ra loa, cũng thông qua QIODevice để bạn ghi dữ liệu vào.

Call/mic → QAudioSource + QAudioOutput
Nhạc/video → QMediaPlayer (+ QAudioOutput)
Hiệu ứng ngắn → QSoundEffect